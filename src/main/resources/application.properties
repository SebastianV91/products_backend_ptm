spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=12345678
spring.datasource.url=jdbc:mysql://localhost:3306/crud_app?useSSL=true&requireSSL=false&serverTimezone=UTC


spring.jpa.hibernate.ddl-auto=update
# Ensure Spring Boot runs the schema and data SQL files at startup
spring.datasource.initialization-mode=always

# This ensures that data.sql is executed during startup
spring.jpa.show-sql=true

server.port=8084

spring.application.name=products
